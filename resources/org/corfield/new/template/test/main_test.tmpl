(ns {{top/ns}}.{{main/ns}}-test
  (:require [clojure.edn :as edn]
            [clojure.java.io :as io]
            [clojure.spec.alpha :as s]
            [{{test-runner/namespace}} :refer [{{test-runner/deftest}} {{test-runner/is}} {{test-runner/testing}}]]
            [org.corfield.new] ; for the Specs
            [{{top/ns}}.{{main/ns}} :as sut])) ; system under test

({{test-runner/deftest}} valid-template-test
  ({{test-runner/testing}} "template.edn is valid."
    (let [template (edn/read-string (slurp (io/resource "{{top/file}}/{{main/file}}/template.edn")))]
      ({{test-runner/is}}
       (s/valid? :org.corfield.new/template template)
       (s/explain-str :org.corfield.new/template template)))))
